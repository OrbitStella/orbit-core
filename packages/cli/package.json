{
  "name": "@orbit-core/cli",
  "version": "1.0.0",
  "description": "CLI tool for Orbit Core deployment and interaction",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "orbit": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@orbit-core/sdk": "workspace:*",
    "commander": "^11.1.0",
    "chalk": "^5.3.0",
    "inquirer": "^9.2.11"
  },
  "devDependencies": {
    "typescript": "workspace:*",
    "@types/node": "^20.0.0"
  }
}
